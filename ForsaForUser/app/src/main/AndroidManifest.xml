<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.forsaforuser"
    android:targetSandboxVersion="1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.AcvityDialog"></activity>
        <activity android:name=".ui.AboutAppActivity" />
        <activity android:name=".ui.AboutUsActivity" />
        <activity android:name=".ui.UpdateAccountActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.CreateAccountActivity" />
        <activity android:name=".ui.SubmitActivity" />
        <activity android:name=".ui.PostActivity" />
        <activity android:name=".ui.CompanyProfileActivity" />
        <activity android:name=".ui.UserProfileActivity" />
        <activity android:name=".ui.ArchiveActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>